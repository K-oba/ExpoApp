<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kaoba.expocr">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".estimote.BeaconAppManager"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.WelcomeActivity"
            android:exported="true"
            android:label="@string/title_activity_welcome" />
        <activity
            android:name=".activities.RegisterUserActivity"
            android:label="@string/app_name" />
        <activity android:name=".activities.BrochureActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ShowStandActivity"
            android:exported="true"
            android:label="@string/title_activity_show_stand"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.LiveExpositionsActivity"
            android:exported="true"
            android:label="@string/title_activity_list_live_exposition"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.ViewExpoCalendarActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme"
            android:exported="true"
            />
        <activity
            android:name=".activities.ViewExpoByFilter"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ShowExpoActivity"
            android:label="@string/title_activity_show_expo"
            android:theme="@style/AppTheme"
            android:exported="true"/>
        <activity
            android:name=".activities.ListStandActivity"
            android:label="@string/title_activity_stand_list"
            android:theme="@style/AppTheme"
            android:exported="true"
            />

    </application>

</manifest>
